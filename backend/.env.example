# FitZone environment variables — copy to .env and fill in
# NEVER commit the real .env

MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/fitzone?retryWrites=true&w=majority
PORT=5000
JWT_SECRET=change_this_to_a_long_random_string
NODE_ENV=development